info:
  name: Admin List Payment Methods
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url}}/admin/payment-methods"
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
