info:
  name: Admin List Payments
  type: http
  seq: 14

http:
  method: GET
  url: "{{base_url}}/admin/payments"
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
