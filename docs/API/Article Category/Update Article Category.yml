info:
  name: Update Article Category
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{base_url}}/article-categories/{{article_category_id}}"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "name": "Company News"
      }

runtime:
  assertions:
    - name: Status is successful
      value: res.status >= 200 && res.status < 300
    - name: Response is success
      value: res.body.status == "success"
    - name: Name updated
      value: res.body.data.name == "Company News"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
