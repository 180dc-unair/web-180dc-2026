info:
  name: List Payment Methods
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/payment-methods"
  headers:
    - name: Accept
      value: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
