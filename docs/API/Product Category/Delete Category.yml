info:
  name: Delete Category
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/product-categories/id"
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{token}}

runtime:
  assertions:
    - expression: ""
      operator: eq
      value: undefined res.status == 200
    - expression: ""
      operator: eq
      value: undefined res.body.status == "success"
    - expression: ""
      operator: eq
      value: undefined res.body.data == null

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
