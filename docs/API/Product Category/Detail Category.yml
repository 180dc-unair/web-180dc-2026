info:
  name: Detail Category
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/product-categories/digital-products"
  headers:
    - name: Accept
      value: application/json

runtime:
  assertions:
    - expression: ""
      operator: eq
      value: undefined res.status == 200
    - expression: ""
      operator: eq
      value: undefined res.body.data.slug != null

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
